Информативные комментарии:
1.
# Создаём новый элемент словаря через доступ по ключу
english['голова'] = 'head'  
2. 
# Добавим в словарь english элементы словаря new_words
english.update(new_words)

  
Представление намерений:
1.
url = 'https://yandex.ru/search/?text=%D0%BA%D0%B0%D0%BA%20%D0%B1%D0%B5%D1%81%D0%BF%D0%BB%D0%B0%D1%82%D0%BD%D0%BE%20%D0%B5%D0%B7%D0%B4%D0%B8%D1%82%D1%8C%20%D0%BD%D0%B0%20%D1%82%D0%B0%D0%BA%D1%81%D0%B8'

# чтобы вычленить текст вопроса
# разбейте строку по знаку = и возьмите
# второй элемент получившегося списка 
2.
# В этом файле может быть несколько переменных
# Каждая - на отдельной строке
# LOGIN=test
# PASSWORD=some_password 
№ TOKEN=zxc123

  
Пояснение:
1.
# вот мы узнали, что код ответа 200 и заполучили это число в свой код:
code = response.status_code
print(f'Код ответа = {code}')
2.
utc_time = dt.datetime.utcnow()

# Создаём промежуток времени в три часа
period = dt.timedelta(hours=3)

# И прибавляем к значению времени по UTC поправку в три часа:
moscow_time = utc_time + period
Предупреждение о последствиях:
1.
#moscow_time = utc_time + 3 - «нельзя складывать переменную типа datetime с переменной типа int».
2.
# Не изменяйте следующий код
print('Привет, я Анфиса!')
print(process_anfisa('Сколько у меня друзей?'))
print(process_anfisa('Кто все мои друзья?'))
Усиления:
1.
# Так не надо делать
updater = Updater(token='1896711190:AAFmXXXXPEn6KqewJ13kKKtlnBYqVC_XXXX')
2.

TODO:
1.
url = 'https://wttr.in'

weather_parameters = {
    '0': '', 
    'T': '',
    'M': '',
    'lang': 'ru'
    # TODO добавьте параметр запроса `T`, чтобы вернулся чёрно-белый текст
}

2. 
# Вынести токен в секреты(dotenv)
